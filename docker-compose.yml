services:
  lint:
    image: buildkite/plugin-linter
    command: ['--id', 'theopenlane/git']
    volumes:
      - ".:/plugin:ro"
  tests:
    image: buildkite/plugin-tester
    volumes:
      - ".:/plugin"
